/*#include <Arduino.h>

void setup() {        // 가장 먼저 한 번만 실행되는 초기화 함수
  Serial.begin(9600); // UART 직렬 통신을 시작. 9600은 **보드와 컴퓨터 간 통신 속도(bps)**
}

void loop() {                       // 무한 반복 실행되는 주 루프
  Serial.println("Hello World !!"); // "Hello World !!"라는 문자열을 시리얼 모니터로 전송하고 줄바꿈
  delay(1000);                      // 1000ms(1초) 동안 프로그램 실행을 멈춤               
}*/